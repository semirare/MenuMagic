<div class="flex flex-col items-center gap-5">
    <p class="text-2xl">Grocery List</p>
    <% @ingredients.each do |ingredient| %>
        <%= render 'grocery_list_item', name: ingredient[:name], quantity: ingredient[:quantity], unit: ingredient[:unit] %>
    <% end %>
</div>