<div class="mx-auto md:w-2/3 w-full flex flex-col">
  <h1 class="font-bold text-4xl">Editing <%= "#{@recipe.name}" %></h1>

  <%= render "form", recipe: @recipe %>

  <%= link_to "Back to Recipe List", recipes_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
