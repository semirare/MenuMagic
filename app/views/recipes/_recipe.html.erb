<%= link_to edit_recipe_path(recipe), class: "w-full" do %>
  <div id="<%= dom_id recipe %>", class="border border-blue-500 border-2 rounded-xl mt-2">
    <div class="text-2xl flex justify-center"><%= recipe.name %></div>
    <hr class="border-t-2 border-blue-500">
    <div class="flex flex-col">
      <% recipe.ingredients.each do |ingredient| %>
        <p><%= ingredient.name %></p>
      <% end %>
    </div>
  </div>
<% end %>